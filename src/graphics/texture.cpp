#include "texture.h"

using namespace Game;

bool Texture::loadFromFile(const std::string& filename, const SDL_Renderer* renderer)
{
    if (renderer == nullptr)
        return false;

    return false;
}
